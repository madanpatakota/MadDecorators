import { DuplicateRemoverPipe } from './duplicate-remover.pipe';

describe('DuplicateRemoverPipe', () => {
  it('create an instance', () => {
    const pipe = new DuplicateRemoverPipe();
    expect(pipe).toBeTruthy();
  });
});
